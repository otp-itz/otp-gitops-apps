apiVersion: batch/v1
kind: Job
metadata:
  name: wait-job-{{.Values.syncWave}}
  namespace: sync-wave-demo
spec:
  template:
    spec:
      containers:
        - name: wait
          image: quay.io/congxdev/wait:0.1
          imagePullPolicy: Always
      restartPolicy: Never
  backoffLimit: 2